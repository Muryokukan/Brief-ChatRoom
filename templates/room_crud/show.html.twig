    
    <div class="relative p-6 bg-white rounded-lg shadow-md border border-gray-200">
  
  <h1 class="text-2xl font-bold text-gray-800 mb-2">{{ room.name }}</h1>
  <hr class="my-4 border-gray-300">
  <div class="text-gray-600 mb-1 text-1xl">
    {% if room.details %}
      <span class="font-bold">Détails de la room :</span><br>
      {{ room.details }}
    {% else %}
      <span class="text-opacity-75 italic">(Aucune description)</span>
    {% endif %}
  </div>
  
  <div class="flex flex-col">
    {{ include('room_crud/_delete_form.html.twig') }}

    <a href="{{ path('app_room', {'roomId': room.id}) }}"
      class="">
      Aller à la Room
    </a>
    <a href="{{ path('app_room_c_r_u_d_edit', {'id': room.id}) }}">
      Modifier
    </a>
  </div>
</div>